// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"project/ent/kitchenresource"
	"project/ent/order"
	"project/ent/predicate"
	"project/ent/resourceallocation"
	"project/ent/shift"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ResourceAllocationQuery is the builder for querying ResourceAllocation entities.
type ResourceAllocationQuery struct {
	config
	ctx          *QueryContext
	order        []resourceallocation.OrderOption
	inters       []Interceptor
	predicates   []predicate.ResourceAllocation
	withResource *KitchenResourceQuery
	withOrder    *OrderQuery
	withShift    *ShiftQuery
	withFKs      bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResourceAllocationQuery builder.
func (raq *ResourceAllocationQuery) Where(ps ...predicate.ResourceAllocation) *ResourceAllocationQuery {
	raq.predicates = append(raq.predicates, ps...)
	return raq
}

// Limit the number of records to be returned by this query.
func (raq *ResourceAllocationQuery) Limit(limit int) *ResourceAllocationQuery {
	raq.ctx.Limit = &limit
	return raq
}

// Offset to start from.
func (raq *ResourceAllocationQuery) Offset(offset int) *ResourceAllocationQuery {
	raq.ctx.Offset = &offset
	return raq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (raq *ResourceAllocationQuery) Unique(unique bool) *ResourceAllocationQuery {
	raq.ctx.Unique = &unique
	return raq
}

// Order specifies how the records should be ordered.
func (raq *ResourceAllocationQuery) Order(o ...resourceallocation.OrderOption) *ResourceAllocationQuery {
	raq.order = append(raq.order, o...)
	return raq
}

// QueryResource chains the current query on the "resource" edge.
func (raq *ResourceAllocationQuery) QueryResource() *KitchenResourceQuery {
	query := (&KitchenResourceClient{config: raq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := raq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := raq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resourceallocation.Table, resourceallocation.FieldID, selector),
			sqlgraph.To(kitchenresource.Table, kitchenresource.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, resourceallocation.ResourceTable, resourceallocation.ResourceColumn),
		)
		fromU = sqlgraph.SetNeighbors(raq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrder chains the current query on the "order" edge.
func (raq *ResourceAllocationQuery) QueryOrder() *OrderQuery {
	query := (&OrderClient{config: raq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := raq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := raq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resourceallocation.Table, resourceallocation.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, resourceallocation.OrderTable, resourceallocation.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(raq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryShift chains the current query on the "shift" edge.
func (raq *ResourceAllocationQuery) QueryShift() *ShiftQuery {
	query := (&ShiftClient{config: raq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := raq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := raq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resourceallocation.Table, resourceallocation.FieldID, selector),
			sqlgraph.To(shift.Table, shift.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, resourceallocation.ShiftTable, resourceallocation.ShiftColumn),
		)
		fromU = sqlgraph.SetNeighbors(raq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ResourceAllocation entity from the query.
// Returns a *NotFoundError when no ResourceAllocation was found.
func (raq *ResourceAllocationQuery) First(ctx context.Context) (*ResourceAllocation, error) {
	nodes, err := raq.Limit(1).All(setContextOp(ctx, raq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{resourceallocation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (raq *ResourceAllocationQuery) FirstX(ctx context.Context) *ResourceAllocation {
	node, err := raq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResourceAllocation ID from the query.
// Returns a *NotFoundError when no ResourceAllocation ID was found.
func (raq *ResourceAllocationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = raq.Limit(1).IDs(setContextOp(ctx, raq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{resourceallocation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (raq *ResourceAllocationQuery) FirstIDX(ctx context.Context) int {
	id, err := raq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResourceAllocation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResourceAllocation entity is found.
// Returns a *NotFoundError when no ResourceAllocation entities are found.
func (raq *ResourceAllocationQuery) Only(ctx context.Context) (*ResourceAllocation, error) {
	nodes, err := raq.Limit(2).All(setContextOp(ctx, raq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{resourceallocation.Label}
	default:
		return nil, &NotSingularError{resourceallocation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (raq *ResourceAllocationQuery) OnlyX(ctx context.Context) *ResourceAllocation {
	node, err := raq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResourceAllocation ID in the query.
// Returns a *NotSingularError when more than one ResourceAllocation ID is found.
// Returns a *NotFoundError when no entities are found.
func (raq *ResourceAllocationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = raq.Limit(2).IDs(setContextOp(ctx, raq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{resourceallocation.Label}
	default:
		err = &NotSingularError{resourceallocation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (raq *ResourceAllocationQuery) OnlyIDX(ctx context.Context) int {
	id, err := raq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResourceAllocations.
func (raq *ResourceAllocationQuery) All(ctx context.Context) ([]*ResourceAllocation, error) {
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryAll)
	if err := raq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResourceAllocation, *ResourceAllocationQuery]()
	return withInterceptors[[]*ResourceAllocation](ctx, raq, qr, raq.inters)
}

// AllX is like All, but panics if an error occurs.
func (raq *ResourceAllocationQuery) AllX(ctx context.Context) []*ResourceAllocation {
	nodes, err := raq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResourceAllocation IDs.
func (raq *ResourceAllocationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if raq.ctx.Unique == nil && raq.path != nil {
		raq.Unique(true)
	}
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryIDs)
	if err = raq.Select(resourceallocation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (raq *ResourceAllocationQuery) IDsX(ctx context.Context) []int {
	ids, err := raq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (raq *ResourceAllocationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryCount)
	if err := raq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, raq, querierCount[*ResourceAllocationQuery](), raq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (raq *ResourceAllocationQuery) CountX(ctx context.Context) int {
	count, err := raq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (raq *ResourceAllocationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryExist)
	switch _, err := raq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (raq *ResourceAllocationQuery) ExistX(ctx context.Context) bool {
	exist, err := raq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResourceAllocationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (raq *ResourceAllocationQuery) Clone() *ResourceAllocationQuery {
	if raq == nil {
		return nil
	}
	return &ResourceAllocationQuery{
		config:       raq.config,
		ctx:          raq.ctx.Clone(),
		order:        append([]resourceallocation.OrderOption{}, raq.order...),
		inters:       append([]Interceptor{}, raq.inters...),
		predicates:   append([]predicate.ResourceAllocation{}, raq.predicates...),
		withResource: raq.withResource.Clone(),
		withOrder:    raq.withOrder.Clone(),
		withShift:    raq.withShift.Clone(),
		// clone intermediate query.
		sql:  raq.sql.Clone(),
		path: raq.path,
	}
}

// WithResource tells the query-builder to eager-load the nodes that are connected to
// the "resource" edge. The optional arguments are used to configure the query builder of the edge.
func (raq *ResourceAllocationQuery) WithResource(opts ...func(*KitchenResourceQuery)) *ResourceAllocationQuery {
	query := (&KitchenResourceClient{config: raq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	raq.withResource = query
	return raq
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (raq *ResourceAllocationQuery) WithOrder(opts ...func(*OrderQuery)) *ResourceAllocationQuery {
	query := (&OrderClient{config: raq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	raq.withOrder = query
	return raq
}

// WithShift tells the query-builder to eager-load the nodes that are connected to
// the "shift" edge. The optional arguments are used to configure the query builder of the edge.
func (raq *ResourceAllocationQuery) WithShift(opts ...func(*ShiftQuery)) *ResourceAllocationQuery {
	query := (&ShiftClient{config: raq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	raq.withShift = query
	return raq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		StartTime time.Time `json:"start_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResourceAllocation.Query().
//		GroupBy(resourceallocation.FieldStartTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (raq *ResourceAllocationQuery) GroupBy(field string, fields ...string) *ResourceAllocationGroupBy {
	raq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResourceAllocationGroupBy{build: raq}
	grbuild.flds = &raq.ctx.Fields
	grbuild.label = resourceallocation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		StartTime time.Time `json:"start_time,omitempty"`
//	}
//
//	client.ResourceAllocation.Query().
//		Select(resourceallocation.FieldStartTime).
//		Scan(ctx, &v)
func (raq *ResourceAllocationQuery) Select(fields ...string) *ResourceAllocationSelect {
	raq.ctx.Fields = append(raq.ctx.Fields, fields...)
	sbuild := &ResourceAllocationSelect{ResourceAllocationQuery: raq}
	sbuild.label = resourceallocation.Label
	sbuild.flds, sbuild.scan = &raq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResourceAllocationSelect configured with the given aggregations.
func (raq *ResourceAllocationQuery) Aggregate(fns ...AggregateFunc) *ResourceAllocationSelect {
	return raq.Select().Aggregate(fns...)
}

func (raq *ResourceAllocationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range raq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, raq); err != nil {
				return err
			}
		}
	}
	for _, f := range raq.ctx.Fields {
		if !resourceallocation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if raq.path != nil {
		prev, err := raq.path(ctx)
		if err != nil {
			return err
		}
		raq.sql = prev
	}
	return nil
}

func (raq *ResourceAllocationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResourceAllocation, error) {
	var (
		nodes       = []*ResourceAllocation{}
		withFKs     = raq.withFKs
		_spec       = raq.querySpec()
		loadedTypes = [3]bool{
			raq.withResource != nil,
			raq.withOrder != nil,
			raq.withShift != nil,
		}
	)
	if raq.withResource != nil || raq.withOrder != nil || raq.withShift != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, resourceallocation.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResourceAllocation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResourceAllocation{config: raq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, raq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := raq.withResource; query != nil {
		if err := raq.loadResource(ctx, query, nodes, nil,
			func(n *ResourceAllocation, e *KitchenResource) { n.Edges.Resource = e }); err != nil {
			return nil, err
		}
	}
	if query := raq.withOrder; query != nil {
		if err := raq.loadOrder(ctx, query, nodes, nil,
			func(n *ResourceAllocation, e *Order) { n.Edges.Order = e }); err != nil {
			return nil, err
		}
	}
	if query := raq.withShift; query != nil {
		if err := raq.loadShift(ctx, query, nodes, nil,
			func(n *ResourceAllocation, e *Shift) { n.Edges.Shift = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (raq *ResourceAllocationQuery) loadResource(ctx context.Context, query *KitchenResourceQuery, nodes []*ResourceAllocation, init func(*ResourceAllocation), assign func(*ResourceAllocation, *KitchenResource)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ResourceAllocation)
	for i := range nodes {
		if nodes[i].kitchen_resource_allocations == nil {
			continue
		}
		fk := *nodes[i].kitchen_resource_allocations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(kitchenresource.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "kitchen_resource_allocations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (raq *ResourceAllocationQuery) loadOrder(ctx context.Context, query *OrderQuery, nodes []*ResourceAllocation, init func(*ResourceAllocation), assign func(*ResourceAllocation, *Order)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ResourceAllocation)
	for i := range nodes {
		if nodes[i].resource_allocation_order == nil {
			continue
		}
		fk := *nodes[i].resource_allocation_order
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(order.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "resource_allocation_order" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (raq *ResourceAllocationQuery) loadShift(ctx context.Context, query *ShiftQuery, nodes []*ResourceAllocation, init func(*ResourceAllocation), assign func(*ResourceAllocation, *Shift)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ResourceAllocation)
	for i := range nodes {
		if nodes[i].shift_allocations == nil {
			continue
		}
		fk := *nodes[i].shift_allocations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shift.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "shift_allocations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (raq *ResourceAllocationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := raq.querySpec()
	_spec.Node.Columns = raq.ctx.Fields
	if len(raq.ctx.Fields) > 0 {
		_spec.Unique = raq.ctx.Unique != nil && *raq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, raq.driver, _spec)
}

func (raq *ResourceAllocationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(resourceallocation.Table, resourceallocation.Columns, sqlgraph.NewFieldSpec(resourceallocation.FieldID, field.TypeInt))
	_spec.From = raq.sql
	if unique := raq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if raq.path != nil {
		_spec.Unique = true
	}
	if fields := raq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, resourceallocation.FieldID)
		for i := range fields {
			if fields[i] != resourceallocation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := raq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := raq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := raq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := raq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (raq *ResourceAllocationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(raq.driver.Dialect())
	t1 := builder.Table(resourceallocation.Table)
	columns := raq.ctx.Fields
	if len(columns) == 0 {
		columns = resourceallocation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if raq.sql != nil {
		selector = raq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if raq.ctx.Unique != nil && *raq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range raq.predicates {
		p(selector)
	}
	for _, p := range raq.order {
		p(selector)
	}
	if offset := raq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := raq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ResourceAllocationGroupBy is the group-by builder for ResourceAllocation entities.
type ResourceAllocationGroupBy struct {
	selector
	build *ResourceAllocationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ragb *ResourceAllocationGroupBy) Aggregate(fns ...AggregateFunc) *ResourceAllocationGroupBy {
	ragb.fns = append(ragb.fns, fns...)
	return ragb
}

// Scan applies the selector query and scans the result into the given value.
func (ragb *ResourceAllocationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ragb.build.ctx, ent.OpQueryGroupBy)
	if err := ragb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResourceAllocationQuery, *ResourceAllocationGroupBy](ctx, ragb.build, ragb, ragb.build.inters, v)
}

func (ragb *ResourceAllocationGroupBy) sqlScan(ctx context.Context, root *ResourceAllocationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ragb.fns))
	for _, fn := range ragb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ragb.flds)+len(ragb.fns))
		for _, f := range *ragb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ragb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ragb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResourceAllocationSelect is the builder for selecting fields of ResourceAllocation entities.
type ResourceAllocationSelect struct {
	*ResourceAllocationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ras *ResourceAllocationSelect) Aggregate(fns ...AggregateFunc) *ResourceAllocationSelect {
	ras.fns = append(ras.fns, fns...)
	return ras
}

// Scan applies the selector query and scans the result into the given value.
func (ras *ResourceAllocationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ras.ctx, ent.OpQuerySelect)
	if err := ras.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResourceAllocationQuery, *ResourceAllocationSelect](ctx, ras.ResourceAllocationQuery, ras, ras.inters, v)
}

func (ras *ResourceAllocationSelect) sqlScan(ctx context.Context, root *ResourceAllocationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ras.fns))
	for _, fn := range ras.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ras.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ras.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
